version: '3.8'

services:
  nginx:
    container_name: nginx
    image: docker.io/nginx
    restart: unless-stopped
    networks:
      - traefik
    ports:
      - 8004:80
    labels:
      - traefik.enable=true
      - traefik.http.routers.nginx.rule=Host(`${DOMAIN}`)
      - traefik.http.routers.nginx.entrypoints=http
      - traefik.http.services.nginx.loadbalancer.server.port=8004
    environment:
      - TZ=Europe/Berlin
      - NGINX_HOST=${DOMAIN}
      - NGINX_PORT=80

networks:
  traefik:
    name: traefik